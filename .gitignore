# Secrets — NUNCA versionar
.secrets.env
*.env
!.env.example

# Docs de dinero/negocio — NO al repo compartido (el amigo hace pull y los ve)
PROPUESTA-LEO.md
PROPUESTA-LEO.docx
NEGOCIO/

# Literatura — PDFs pesados/copyright quedan LOCAL; el INDEX + notas SI se versionan
literature/pdfs/
literature/**/*.pdf

# Datos / pesos / checkpoints (van en RunPod, no aqui)
data/
datasets/
*.ckpt
*.safetensors
*.bin
checkpoints/
runs/
wandb/

# Obsidian — config local del editor (el vault ES el repo; los .md se versionan, el workspace no)
.obsidian/

# Basura
__pycache__/
*.pyc
.DS_Store
