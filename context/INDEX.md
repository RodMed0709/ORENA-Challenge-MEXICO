# context/INDEX.md — the brain (start here)

> Root map of what we know and where it lives. Obsidian-style: this file is **short**;
> it POINTS, it does not CONTAIN. Read it BEFORE proposing an architecture change, a
> model swap, or a new experiment — so we don't re-litigate settled calls.
>
> **Workflow:** local-first → push to GitHub. Never `pull` on the pod while it trains
> (avoid stepping on a teammate's run). GSD = how we *do* work; this brain = what we *know*.

## HAS THIS BEEN MEASURED? — ask here FIRST
- **`context/MEASURED.md`** — 🤖 **GENERATED** (`python -m frame.measured`, never hand-edited). One table of every question this project has already answered: settled verdicts, what each rung changed, which runs produced a number, and **which stratified cuts are already computed**. **Read it before proposing an experiment.** Four sessions in a row re-derived work that was already committed — not for lack of documentation, but for lack of a way to ask. Rows marked ⚠️ have been narrowed or partly withdrawn: **read their note, not the row.**

## NOW — what's happening
- **`context/NOW.md`** — the living current-state: fronts, real numbers, findings, what's in progress. **Read it first.** (`HANDOFF.md` is the older baseline-run handoff, kept as history.)
- Live fronts:
  - **Leo (legokna)** → rung 06 **ViT-LoRA** — scored, verdict **PARTIAL** ([[vit-lora-partial]]); next is a lower `vit_lr`. See [[NOW]].
  - **Rodrigo** → MLOps consistency system + **R1 CoA-format SFT** — see [[next-move-rodrigo-coa-format]].

## STRATEGY — the north star
- **`THE_MAP.md`** — unified FRAME strategy: the 4 buckets, Block A/B, §0 roadmap + Phase-3 status, Block-A diagnosis (folded from the retired `BLOCK_A_MODEL.md`).
- **`ATTACK_LADDER.md`** — original master ladder (recipes valid; THE_MAP supersedes the sequencing).
- **`CONSTITUTION.md`** — hard rules, version pins, structure §VIII–IX.

## RULES — how we stay consistent (read before eval/training/results)
- **`context/RULES.md`** — the DO/DON'T checklist that stops recurring mistakes (human or Claude). EVAL rules: score only via `frame.metrics`, leaf→group via `Capability.group`, ID/OOD from qID, headline `bucket_mean`, gates RAISE (never disable). Complements `CONSTITUTION.md`.

## DECISIONS — settled verdicts (do NOT re-litigate)
- [[viT-swap-nogo]] — swapping Qwen3-VL's ViT = **NO-GO** (license + full realignment).
- [[qwen-size-ladder]] — next size up = **30B-A3B MoE FP8** (measured wildcard, not the cheap step). 🔒 Gated by [[vit-lora-partial]].
- [[unused-metadata]] — 🔴 **90% of questions carry `secondary_capabilities` we never read**: aggregation supervision is **+77% larger** than we thought (9,762 train questions, not 5,524) at zero annotation cost. Also `generation` (automatic/anchor/manual) unused, and **`clinical_relevance` is all-False** — a second landmine beside `ood`.
- [[leaderboard-metric-vs-our-headline]] — 🔴 **read before quoting any number as "our score".** `pre_evaluation_score` is an unweighted mean over **populated** buckets and only **2** populate on pre-eval, both ID — so it is NOT our 4-bucket `bucket_mean` (the right comparator is mean-ID: 0.5281, not 0.5667). Exact rationals recover **B = 2000 questions over 20 videos** and prove we and the 4B were scored on the **identical set**: there a **4B beats us 0.5163 vs 0.4710**, all of it `aggregation` (343 vs 410 of 754), `object_recognition` a **dead tie** (607 vs 609). Latency is a non-issue (0.79 s/q vs a 5.06 ceiling; `mean_latency_s × throughput = 20.0` exactly). The **final** ranking is Copeland + significance tests, ID and OOD weighted equally.
- [[aggregation-is-the-gap]] — 🔴 **THE target.** First external reference: a **4B model beats us on `aggregation`** (50% of the exam). ⚠️ **PARTIALLY RETRACTED 2026-07-27** — the "+14.9 lead on `object_recognition`" compared our *local val* against their *platform* score; on identical questions it is a **two-question tie**, and we are behind **overall** by 4.5 pts. The `aggregation` trail survives and is now measured on the same questions (−67 of 754).
- [[the-gap-is-the-number-format]] — 🔴 **WHERE the gap is.** `aggregation × ID` is **80.4% `number` format**, and `fo_class` is **not in `aggregation` at all** (it sits entirely in `object_recognition`). Arithmetic: answering 100% of `binary` only reaches 0.4492 — **no path to the target avoids lifting `number` 0.327 → ~0.482**. And `number` is the format that clears its floor by only +8.0 pts (+0.013 on OOD) and returns the mode floor on a black image. **Re-scopes [[class-imbalance-not-counting]].** Zero GPU — the number had been committed for days.
- [[latency-budget-is-pooled]] — 🟢 **the 5 s cap is POOLED, not per-question**: `120 s setup + B × 5 s` per run, read from the official template. With p99 = 0.352 s measured, **self-consistency (k-voting) and higher `max_pixels` are affordable** — both were closed against a ceiling that does not exist as modelled. 🔴 **The risk inverts to COLD START**: imports + weight load + CUDA-graph capture all eat the 120 s setup allowance.
- [[count-calibration-dead]] — 🔴 **post-hoc count calibration is DEAD**, measured three ways (05c): oracle LUT gain **+0.026** vs the **+0.155** needed, ID↔OOD transfer **−0.016** (negative), and `argmax_injective` **False**. Mechanism: true values **2, 3 and 4 all share the same modal prediction (1)** — a LUT can only send `pred=1` one place, so it trades one error for another. **Not fixable with more data.** Leaves synthetic-counting SFT as the only lever left in the group that owns the gap.
- [[resolution-is-not-the-gap]] — 🔴 **the resolution axis is CLOSED, at the gate, for zero GPU.** The "100 %/0 % partition" was an artefact of a non-random n=50: over the full 15,213-frame cache `lapchole` (ID) carries **six** resolutions and its minimum (**230k px**) is below `heico`'s uniform 518k — so **"OOD gets 56 % of ID's visual tokens" is wrong** (~66 % by mean, inverted in the tails). Structurally unanswerable anyway: **130/130 videos have exactly one resolution**, so resolution is **perfectly confounded with video identity**. And accuracy across resolution cells is **non-monotonic**, with two cells resting on one video each. Kills 6b, 11b and 11c unrun.
- [[open-class-vocabulary]] — 🔴 **the class set is OPEN** (`overview.md:17`: "such as … and similar objects"); the organizers' predefined list is **10 classes, not 8**, and **`mesh` + `foreign object` have ZERO examples anywhere**. `silicone loop` is train-only. **Never reshape training classes against val frequencies.** Also: 70% of questions carry no class list although the spec says they do — a possible train/test regime mismatch. 🔴 **And the prompt's 10-item list and the scorer's 10-item list disagree on their 10th element** (`foreign object` vs `Absorbable Hemostatic Agent`): an unrecognised `fo_class` token **RAISES** in `verify()` rather than scoring 0 → RULES §8b.
- [[class-imbalance-not-counting]] — ⚠️ **RE-SCOPED: measures `object_recognition` (where we LEAD), not the gap** — see [[the-gap-is-the-number-format]]. Within that bucket: the failure is **per-CLASS, not per-count**: `gallstone` recall **0.000** (14 train ex.), `needle` 0.511, and a **phantom class** (`silicone loop`, 435 train ex., 0 in val) that is a guaranteed FP. **The ViT LoRA DID work** — `needle` +17.8 pts — `bucket_mean` hid it. But **68% of misses are on classes with >400 examples**, so rebalancing has a low ceiling.
- [[checkpoint-selection-vs-number]] — training erodes `number` on OOD **in both arms, so it is not the ViT** — killed a 7.5 h `vit_lr` run before it was spent. ⚠️ **Its selection claim is RETRACTED**: measured on OOD only; on the full 6252 epoch 2 wins and **RULES §6 is CONFIRMED**.
- [[vit-lora-partial]] — LoRA on the ViT = **PARTIAL**. Settles the *reading*: it neither licenses nor kills the capacity branch. **The ceiling question stays OPEN** until a lower `vit_lr` runs.
- [[next-move-rodrigo-coa-format]] — Rodrigo owns **CoA-format SFT (R1)**; RL is **deferred**.
- [[eval-canonical]] — ONE scoring module (`frame.metrics`): leaf→group via `Capability.group`, ID/OOD from qID; headline = `bucket_mean`, pre_eval reference-only.
- [[no-external-api-for-challenge-data]] — 🔒 **BINDING (DUA):** NO challenge frames/annotations to any external API (Gemini/GPT/Claude/DeepSeek, incl. MCP). Data-touching models must have downloadable weights and run **on-pod**; only Qwen is released, the generator is documented not shipped. Qualifies RULES (new §COMPLIANCE).
- [[coa-generator-qwen32b-onpod]] — R1's scaffold generator = **Qwen3-VL-32B, vision, zero-shot, on-pod** (Apache-2.0, same family as the 8B student). Supersedes the text-only "no pixels" line. Gate: eyeball-50-vision first, **matched 2k-bare control**, emit-only-`<answer>` eval + p99, number-aware selection. (CoA's own teacher was an external API → DUA-forbidden for us, so on-pod is now *required*, not merely chosen.)
- [[coa-sft-published-null]] — 🔴 **the "+16.3 is the format, R1 is the untested cell" premise is FALSIFIED.** The source (arXiv:2603.20116, **our exact backbone**) *does* contain the no-RL scaffold row: **62.0 vs bare-gold SFT's 65.7** (EndoVis2018) — a published wash that loses; the gain is **RLVR's**. Same paper: SFT crushes class-balanced F1cls 20.7→15.3 = **our `clip` attractor, published** (we report no class-balanced metric). Rung 09 **re-scoped, not cancelled**; the one empty cell left is **emit-vs-suppress the trace on one checkpoint**. Retires "+16.3" everywhere.
- [[covt-reduced-sam-route]] — 🔴 **CoVT as published = NO-GO, and it corrects how we score the board.** On a Qwen backbone its gain is **Depth +14.0 / Dist +7.0 and Count +1.2** — the advertised +26.6% BLINK-count is **LLaVA-13B vs Aurora**, not Qwen. Blocked by a **774.6k-row corpus**, 17K steps, and a pipeline no recipe framework expresses (ms-swift, LLaMA-Factory *and* Unsloth alike — switching does not buy it). 🎯 **But the headline is the mean of the two ID buckets, so `object_recognition × ID` is 50% of the score, and leaves `1d`/`1e` spatial_localization live inside it** — CoVT is badly aimed at `aggregation` and *well* aimed at half the headline. **"The gap is counting" is true about the deficit and false about where points are available.** What survives: the interior optimum (8 ≫ 32 tokens), **fully automatic mask GT with no human annotator** (A.2), and a bespoke **SAM 2** route — text needs a detector in front (SAM 2 is *visual*-promptable only), its **video/streaming memory fits our data**, and its own **model-in-the-loop data engine** is the bootstrap precedent. 🎯 First probe decomposes the **±0.86** without clinicians. **Blocked: no videos in local.**
- [[synthetic-counting-reconciled]] — 🔴 **reconciles a week-old contradiction between two committed records.** `CAMPAIGN_LOG.md:240` discarded synthetic counting; `count-calibration-dead` called it "the sole remaining lever" — that is survival **by elimination, not merit**, and it has been misread as promotion twice. **DISCARDED stands.** Also settles what **±0.86** is (mean |Δ| in the gold clip count between annotated frames <1 s apart, 1,946 pairs, no free parameters — but an **upper bound** that cannot separate scene change from annotation error). 🔴 **And the missed fact: rung 15 already ran the label-supported half of v05 / point-then-count → null.** The pointing arm needs localization labels **the dataset does not have**; any pseudo-label route (SAM points, CoVT tokens) must agree with a gold that moves ±0.86 → a self-contradicting target. **The whole `aggregation` branch is gated on label trust, not on method** — and a noise probe built without clinical adjudication would just produce another interpretation-dependent number.
- [[epoch-matched-control]] — 🔴 **rung 06 never evaluated its own ep3**, so rungs 14 and 15 compared their ep3 against its **ep2**. Rung 14 = faithful NULL (best `margin_OOD` +0.0078, CI includes 0); rung 15 ep3 = +0.0032 headline but ID-driven, `margin_OOD` −0.0110, `number` margin OOD **below floor**, and **0/10** paired cells exclude zero. **Rung 06 stays the best checkpoint.** The missing control is merge+eval, ~1 h, zero training — run it before ANY new training, especially before fusing 14+15. ⚠️ **No run has ever been repeated with a different seed** → no variance estimate at all. Adds RULES §6b.
- [[gold-is-signal-model-underuses-it]] — 🟢 **the annotation-ceiling reading is RETIRED.** A blind human glance scores **r = +0.72** against the counting gold across the full 1–12 range (n=106, only 2.8% "cannot tell"). The gold carries real ordered frame-visible signal. Reconciles the old **−0.17** as **range restriction** (it was measured on gold 3–6 only). ⚠️ **PARTLY RETRACTED — every "our model +0.43" in it is withdrawn, see [[model-out-ranks-the-blind-human]].**
- [[undertrained-was-real]] — 🟢 **THE PLATEAU WAS A LEARNING RATE, NOT A DATA CEILING.** One flag — `lr 2e-5 → 1e-4`, zero data change, rung 18's own `train.jsonl` in place — moves `bucket_mean` **0.5721 → 0.6305** and the leaderboard proxy **+0.0480**, epoch-matched. **21 of 30 paired video-clustered cells exclude zero and ALL 21 favour the arm**; rungs 14 and 15 managed 0 of 6. Largest single move of the campaign, and it lifts BOTH scored buckets plus OOD (`margin_OOD` +0.0705). 🔴 Retires the reading that **"training erases OOD counting"** — rung 06's exactly-0.000000 `number` OOD margin was under-training, and at 1e-4 the same schedule gives +0.026. Also: the cosine anneals to **lr 0.0**, so "still rising at epoch 3" meant the schedule ran out — a resume would learn nothing. ⚠️ ONE alternative LR tested (the band is 1e-4–3e-4); rank and 2e-4 are running.
- [[undertrained-on-both-axes]] — ⚠️ **the prior above, now CONFIRMED on the LR axis** (rank still open). Kept as the reasoning, not as a result — every number in it is somebody else's. 🔴 **19 rungs changed data; NOBODY ever changed the optimiser.** Every strong surgical-VQA result pairs lr 1e-5–2e-5 with **15–20 epochs**, or 3–6 epochs with **lr 1e-4–3e-4**. We run **lr 2e-5 for 3 epochs** — the only configuration in the published grid taking BOTH discounts, at ~1/5–1/10 of anyone's optimisation distance. And **rank 8 was never swept** while IOVQA (our data scale) measures rank 32→128 = **+0.028** against model 7B→72B = **−0.020** — the direct rebuttal to "a 4B beats our 8B so capacity is irrelevant". ⚠️ LITERATURE-GROUNDED, UNTESTED — a prior and an experiment design, never a result. Counter-citation on file (Biderman: higher rank forgets more). Licenses `experiments/21-recipe-sweep/`.
- [[loss-mass-is-token-weighted]] — 🔴 **the optimiser weights answers by LENGTH, and our shortest answers are the ones that matter.** `transformers` ≥4.46 (we pin 4.57) normalises CE by `num_items_in_batch` = token-mean over the effective batch, so a row's influence ∝ its answer's token count. Measured on the real `train.jsonl`: **`number` is 34.2% of rows but 20.8% of the gradient (−1.64×)** while `fo_class` takes 62.4% off 50.9%. The format that is **80.4% of the aggregation deficit** gets the least attention — so the dose in ROWS was never the dose in GRADIENT, which reframes rungs 15 and 18's nulls. ⚠️ 1.64×, NOT the 5× the hypothesis estimated. 🟢 **The reduction path is no longer a prior: it was MEASURED on the pod** (`experiments/22-loss-mass/RESULTS_preflight.json`) — the `num_items_in_batch` the trainer receives equals the token sum of all 16 micro-batches, to the token, in every step. ⚠️ The source quotation that "discharged" it was incomplete: `seq2seq_trainer.py:196` sits under `if num_items_in_batch is None:` and would have meant the OPPOSITE (already per-sample at `per_device=1`) had the guard been open. Same physics at ~70–100× is why scaffold-SFT is a published wash.
- [[model-out-ranks-the-blind-human]] — 🔴 **"the human orders better than our model" is FALSE — it compared two different gold ranges.** The 0.43 is the **gold 3–6** number (reproduced at 0.4103); on the human's OWN 109 frames the model scores **r = 0.8303** vs the human's 0.7230, and on the full `Clips` template **0.5866** (ID 0.664 / OOD 0.500, CI [0.467, 0.684]). Retracts the "large discrimination headroom" and the "model emits the prior" readings — a model emitting the marginal cannot score 0.59. 🟢 **What replaces them is sharper: on OOD the model ORDERS at r = 0.4997 and adds EXACTLY 0.000000 exact-match margin** (accuracy == floor to 6 dp) — ordering skill is real and does not convert into score, so a lever that lifts `r` has not thereby lifted `bucket_mean`. Adds RULES §13b/§13c; re-baselines rung 18's pre-registration from 0.43 to **0.5866**.
- [[container-answer-path-audit]] — 🔴 **the shipped container did no normalisation, and a trailing period is AUTO-INCORRECT.** `Number.verify` = `strip().isdigit()`, `Binary.verify` = `in (yes,no)` — so `"1."` and `"Yes."` score wrong, and 16a measured **87%** of `number` answers in that shape on OOD (class, question) pairs — ⚠️ **NOT a paraphrase fragility: 16d measures 0.0000 illegal on real corpus questions across six variants and all three checkpoints**; the trigger is rare/unseen classes (gallstone/needle/silicone-loop score 1.0000, `Clip` 0.654). Fixed with a format-agnostic `normalize_answer` (the container cannot know the format — `Request` has no `answer_format`), verified against the SDK's real verifiers: 7 repairs, **22 inputs byte-identical**. 🟢 The adversarial-abort risk was measured to **0 hits across 146,004 committed answers** and deliberately NOT filtered — the SDK says the detector exists to stop output tuning. ⚠️ `answer_postprocess` exists in the engine and NOT in the container: a fix through the hook does not reach the shipped artifact.
- [[prompt-only-pointing-collapses]] — 🔴 **`len(points)` is dead as an inference-time trick.** Both pointing arms lose to the bare integer in EVERY cell (a1 OOD margin −0.116, a2 −0.139 vs control −0.003), at 3.4× the latency. Mechanism is visible and degenerate: **a2 emits exactly ONE point on all 681 questions** (`mean_pred` 1.0000) — the model does not enumerate, so the derivation reads a constant and AMPLIFIES the undercount (bias −2.52 vs −0.66). ⚠️ Bounds the **prompt-only** path, which Alghisi themselves predicted; their +80.9 came WITH point-supervised LoRA. Stays open as a training target, gated on 16b.
- [[zero-is-format-localized]] — 🔴 **the missing zero is a FORMAT deficit, not a missing capability.** The fine-tuned model says `no` fluently (binary emit_absent 0.82) and **never says `0`** (number 0.008/0.017) — exactly matching supervision that has 1,008 `no` golds and **zero** numeric zeros. The base is NOT a counter-example: it is a zero **attractor** that negates **83% of PRESENT** cases, so HoloCount's 96.4% does not transfer and there is nothing to restore. ⇒ minting is licensed for `number` ONLY, halving the plan. 🔴 **Bigger finding:** fine-tuning created a **format fragility the base lacks** — on off-template phrasings `ft` emits `"1."` on **87%** of number questions, which `Number.verify` **auto-fails** (base: 0.0000). Fixable with one line in the existing `answer_postprocess` hook.
- [[submission-01-rung06]] — 🟢 **first submission shipped** (`Qwen3VL-8B-FT-ViT-LLM-v1`). Exposed a 🔴 **conflict between the platform interface (`/input/batch-frames.zip`) and the organizers' own template (`/input/frames/<qID>.png`)** — the container now accepts both and logs the `/input` inventory, or a silent all-empty `answer.json` would have burned a submission. Also the **ground truth of what rung 06 actually is**, read from `adapter_config.json`: pure LoRA with all base weights frozen, and **two** excluded modules (`visual.merger` + `visual.deepstack_merger_list`), not one.

- [[wrong-judge-model]] — 🔴 **`Qwen/Qwen3.5-4B` EXISTS** (HF, 2026-02-27, 6.4M downloads) and the config comment claiming otherwise is false, so every judged number in this repo came from a substitute (`Qwen3-4B`). The judged formats are **376 of the 1,296 `object_recognition` ID questions** — the bucket that collapsed on the platform. Also surfaces the rubric clash: the judge explicitly rewards *"the correct information … also includes extraneous text"* while our SFT trains terse template-locked strings, and **zero of 19 rungs targeted the judged formats**.
- [[judge-swap-is-not-the-gap]] — 🟢 **MEASURED: the wrong judge cost us nothing.** Re-scoring rung 06 ep3 under both judges in one process moves the headline by **−0.0014** and moves `object_recognition` ID **UP +0.0039**; `aggregation` is byte-identical because none of its questions are judged. Agreement 99.2% overall / 93.5% on judged rows, with **`multiple_choice` 202/202 identical** and all 49 disagreements in `open_ended` (40 of them OOD). ⇒ **the −0.150 platform collapse is real behaviour on unseen centres, not our instrument** — the newer-**generation** backbone branch is the live one. Bonus: the disagreeing rows show the model emitting the right structure and the wrong class, **collapsing toward `Clip`**. Operational: the official judge **cannot load under `transformers` 4.57** (`KeyError: 'qwen3_5'`) → it needs its own venv, which is almost certainly where the false "does not exist" comment came from.
- [[backbone-generation-is-not-the-lever]] — 🔴 **MEASURED: three generations buy +0.036, our own fine-tuning buys +0.317.** `Qwen3.6-27B` zero-shot on the full 6,252 = **0.2913** vs rung 00's 0.2557 and rung 06 ep3's **0.5724** — a gen-3.6 27B does not reach half of our fine-tuned 8B, so the `transformers` 5.x migration (2–3 of the 6 remaining weeks) is **NO-GO** and 23b never runs. ⚠️ The one counter-datum, kept: the lift is **not uniform** — `object_recognition` OOD gains **+0.090**, 2.5× the headline, in exactly the cell that collapsed. Free findings: a 27B runs at **p99 0.851 s** (faster than our 8B — size is not latency-bound), and it emits `0` and `Mesh`, tokens our supervision cannot teach. Also: the 2026 Qwen line is a **reasoning** model — left thinking on it scores 0.0000 by truncation.
- [[counting-is-a-mapping-failure]] — the model **SEES** the objects and cannot **EMIT** the number: Spearman 0.487 at gold ≥5 (0.661 full range), bias ≈ −2. Scalar low-count data does not transfer; point supervision does.
- [[cross-question-constraints]] — 🟢 **questions about the same frame constrain each other and we never used it**: 495 frames pair a per-class count with the total, 979 pair a `number` with a `fo_class`. Free, exact CoA ground truth already inside `train.parquet`. ⚠️ **TRAIN-time only** — at inference the SDK `Request` carries no sibling questions, so it must be distilled into the weights.

## KNOWLEDGE — dense notes (open only when needed)
- **Data card — know your data (READ before quoting any number)** → `experiments/08-data-card/` (README + `tables/*.csv`). The measurement-trap map. Sharpest traps: effective n ≈ **38 videos** (not 6252); the unit is the **TEMPLATE** (188), not `answer_format`; **`acc_number` is NOT interpretable** (8 templates, 4 degenerate — use the hierarchical estimate); **`acc_OOD > acc_ID` is an ARTIFACT** (OOD floor is 12 pts higher → read **MARGIN over the template-aware floor**, not raw acc); `procedure_type`/`generation` reach the model (free stratifiers — but `procedure_type` as a model lever risks OOD, unseen procedures break it). The canonical eval (`src/frame/metrics.py`) already closed its capability-mapping + orphan + two-estimator open items (§12).
- **Campaign log — EVERY lever, its verdict, and the infrastructure built** → `context/CAMPAIGN_LOG.md`. The ledger of 13–22 Jul 2026: comparative tables across every arm we ran (the ladder, the rung-05 image ablation, the 12b unsharp arms, the 12c 25%-subsample pair), the four scored cells, per-format and per-capability breakdowns, the rung verdicts, and the discarded ideas with the evidence that killed them. Sharpest facts: **the model has not improved since rung 06**; **`number` is the only format where rung 06 does NOT beat rung 02**; the ViT moved `object_recognition` (+0.040/+0.030) and `aggregation` **not at all**; **any inference-only test of an INPUT intervention is biased toward the negative** (−0.056 measured at inference vs +0.021 trained); and the headline arithmetic — four equal cells means a `fo_class`-only gain of +0.02 buys **+0.004** of `bucket_mean`, so the question is not "does it help" but "does it move a whole cell". Visual evidence in `docs/viewers/`.
- **Error anatomy — WHAT the model gets wrong, from the committed answers (zero GPU)** → `context/ERROR_ANATOMY.md`. Confusion matrices for `number` and `fo_class` off rungs 00/02/06 `inspect.csv` (which carry the exact `our_answer`). Sharpest facts: **`number`+`binary` ARE all of `object_aggregation`; `fo_class` is NOT in it**; merit read as margin **inverts** the raw ranking (`binary`-OOD +0.046, `number`-OOD +0.013 ≈ trivial, while `fo_class` is the strongest at +0.32); `number` fails by **compression toward the prior** (bias −0.66, 0 % accuracy from gold ≥5, and golds 3–4 share modal prediction 2 — why a LUT cannot work); `fo_class` gets **cardinality right and identity wrong** with **`clip` as the default attractor** (emitted 875× vs 615 golds). 🔴 **The annotation ceiling cannot be measured from this data** — `scene_inventory` is built FROM the golds, so checking golds against it is circular; the two non-circular cross-checks show **≥11 % of counting labels contradict each other**.
- **Evaluations** → `src/frame/metrics.py` + gates (canonical scoring: leaf→group + qID ID/OOD).
- **Results ledger** → `RESULTS.md` + committed `results/` tiers, auto-built by `frame.ledger.build_results_ledger` (never hand-edited, cannot drift). **Tier 1** `results/summary.csv` (one row per experiment/run — headline compare), **Tier 2** `results/detailed.csv` (run × capability_group × {ID,OOD} × answer_format — accuracy/n/floor/CI), **Tier 3** `results/by_run/<experiment>__<run>.csv` (full per-run canonical breakdown). Source = each run's canonical `stratified.json` (dict from `frame.metrics.stratified_report`, dropped via `frame.ledger.register_run`); runs lacking one are Tier-1-only from their `RESULTS.csv` and flagged `needs_backfill` (need a pod pass of `stratified_report` to go rich).
- Per-experiment context: `context/<id>/CONTEXT.md` (`00-baseline` … `08-data-card`).
- Experiment designs: `context/EXPERIMENT_DESIGNS.md`.
- Papers: `literature/INDEX.md` + `literature/FICHAS.md` (the technique fichas).
- **What we do TO THE MODEL** → `literature/vlm-techniques/` (INDEX + 33 fichas over 45 PDFs, tiered against *our* setup; opens with a 5-claim synthesis + a ranked steal-list). Sharpest: **scaffold-in-the-SFT-target without RL is a published wash on our exact backbone** ([[coa-sft-published-null]], replicated by Surgery-R1) and **CoT degrades perception specifically — grounding + object counting** (4 papers agree; counting peaks ~40% with thinking tokens then declines) → emitting the trace is the risky default; **counting is a loss/target problem, not a VLM law** (Qwen2.5-VL-7B + plain LoRA: count MAE 9.86 → 0.26); **forgetting is fixable post-hoc, free, on the epoch-3 checkpoint we already own** (LiNeS keeps 99.8% of task perf while restoring 97.9% of pretrained).
- **What we do TO THE PIXELS** → `literature/preprocessing/` (INDEX + fichas, 49 papers catalogued / 42 PDFs, endoscopic enhancement + preprocessing in front of a VLM). Sharpest: **inference-only input transforms are structurally biased negative** (TTA on a model not trained with it costs up to −31.6 pts) and **enhancement metrics do not predict task metrics** (uniform enhancement *lowers* mAP; gains only per-image selective) — together they explain both of rung 12's headline negatives; the fix for an input-side transform is to **train with it, not apply it at test** (16 endoscope settings swing AI sensitivity 9 pts, augmentation collapses that to 1–2); and the live levers are **geometry, not operators** — attention-guided crop to small details, tiled inference (+5–14 AP), train/inference resolution match, frame-quality gating.
- Challenge spec: `context/challenge/`.

## PROCESS — how we work (GSD / spec-driven)
- `.planning/` — PROJECT / ROADMAP / REQUIREMENTS / STATE + per-phase plans.
- GSD drives the *doing* (phase → plan → execute → verify). This brain holds the *knowing*
  (what we decided and why). The bridge is this INDEX.

## Keeping this alive (rules)
- **New settled verdict** → add one short file in `context/decisions/`, link it here.
- **State changed** → update `HANDOFF.md` (the NOW), NOT this index.
- **Decision file format:** `Question / What we sought / What it gave us / Verdict / Sources`.
- Keep this INDEX to one screen. If it grows, it's carrying content it should only point to.
