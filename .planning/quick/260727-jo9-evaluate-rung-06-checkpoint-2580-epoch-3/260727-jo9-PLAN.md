---
quick_id: 260727-jo9
title: Rung 06 epoch-3 control for rungs 14 and 15
mode: quick
created: 2026-07-27
branch: task/r3-rung16
must_haves:
  truths:
    - Rung 06's checkpoint-2580 has a canonical `bucket_mean` measured on the full 6252-question
      eval set under the protocol rungs 14 and 15 used, so their ep3 rows have a control.
    - The control is measured on the same GPU class (RTX 5090) that produced rungs 14 and 15,
      because ~0.5% of stored answers change on a GPU swap
      (context/decisions/archived-results-not-bit-reproducible.md).
    - The eval protocol is byte-for-byte the one used by `eval_best`: same judge, seed 42,
      max_pixels 1280*720, `answer_postprocess=None`, greedy, full 6252.
  artifacts:
    - experiments/06-vit-lora/06c_epoch3_eval.ipynb
    - experiments/06-vit-lora/RESULTS_epoch3.csv
    - context/decisions/epoch-matched-control.md (verdict section)
  key_links:
    - experiments/06-vit-lora/06b_epoch1_eval.ipynb
    - experiments/02-lora-sft/_models/lora_sft_train.py:254
    - src/frame/run.py
    - src/frame/metrics.py
---

# Quick task 260727-jo9 — the epoch-3 control rung 06 never ran

## Why

`context/decisions/epoch-matched-control.md` records the load-bearing gap: rungs 14 and 15 each
evaluated all three epochs, but **rung 06 stopped at epoch 2**, while its `eval_loss` was still
falling (0.3204 → 0.2925 → 0.2782). Both team rungs therefore compare their ep3 against rung 06's
ep2 — and rung 15's only apparent win (0.5699 vs 0.5667) lives exactly there.

| ckpt | rung 06 | 14 appearance-aug | 15 count-target |
|---|---|---|---|
| 860 (ep1) | 0.5345 | 0.5291 | 0.5274 |
| 1720 (ep2) | **0.5667** | 0.5642 | 0.5612 |
| 2580 (ep3) | **missing — this task** | 0.5611 | 0.5699 |

The decision note pre-registers the read: **if rung 06 ep3 lands ≥ 0.5699, rung 15 dies outright**
(and we may gain a free checkpoint); **if it lands below 0.5667, the ep3 rows of both rungs need
rereading.** Zero training — merge + eval, ~1 h.

## Verified before planning (do not re-derive)

- Adapter exists: `repo/experiments/06-vit-lora/runs/06_vit_lora_v1/ckpt/v0-20260717-224148/checkpoint-2580/`
  on volume `gf78k60nlt`.
- `merged/` holds **only** `checkpoint-1720` — the ep3 merge has to be produced (~17 GB, ~10 min).
  Rungs 14/15 delete each merge after its eval, which is why ep1's merge is gone.
- There is no rung-06-specific exporter: `vit_lora_train.py` has none, so rung 06's own
  `merged/checkpoint-1720` was produced by the shared `lora_sft_train.merge_checkpoint`
  (`swift export --merge_lora`). Using it is faithful, not a substitution.
- Protocol constants are identical across 06b, 14 and 15: `max_pixels = 1280*720`
  (`lora_sft_train.py:57`), `seed = 42` (`:60`), same `run_baseline`, same judge
  (`/workspace/hf_cache/hub/models--Qwen--Qwen3-4B`).
- Pod recipe is on the volume from the R2 wave: `tmp/full/{prep,full_runs,driver}.sh` —
  env `/workspace/envs/infer/bin/python`, `HF_HOME=/workspace/hf_cache`, offline flags,
  `papermill -k python3 --cwd <dir> -p SMOKE False`, driver detached via `nohup setsid`.
- Rungs 14/15 evals took 29–40 min per checkpoint on the RTX 5090 (`events.jsonl`).
- All pods currently EXITED — no billing in flight.

## Tasks

### Task 1 — build `06c_epoch3_eval.ipynb`

**files:** `experiments/06-vit-lora/06c_epoch3_eval.ipynb` (new)

**action:** Clone the structure of `06b_epoch1_eval.ipynb` with three deltas and nothing else:
1. `CKPT = "checkpoint-2580"`, `RUN_TAG = "ep3_full"`.
2. **One arm only** — `b1_vit_lora`. Rung 02's ep3 is deliberately out of scope: the question is
   "what is rung 06 at ep3", and adding rung 02 doubles GPU for a number nobody is waiting on.
3. Reference table compares against ep1/ep2 of rung 06 **and** the ep3 rows of rungs 14 and 15
   (0.5611 / 0.5699), since settling those is the point.

Everything else is carried over verbatim: `parameters`-tagged config cell (`SMOKE`, `RUN_TAG`),
the PATH fix so `swift` resolves from this interpreter's bin, DATA_ROOT probing, merge-if-absent,
`BaselineConfig(max_pixels=1280*720, seed=42, n_eval=40 if SMOKE else None)` with
`answer_postprocess` left at its `None` default, GATE 0 gold coverage, `assert_no_dup_qid` /
`assert_ood_from_qid` / `assert_all_rows_grouped`, `stratified_report(gold=...)`,
`assert_floors_vs_eval_set`, `ledger.register_run` (skipped under SMOKE).

Add one thing 06b lacks: **delete the 17 GB merge in a `finally` once its `results.csv` is scored**,
copying rung 14's disk discipline (`14_appearance_aug.ipynb` cell 12) — an eval that raises is the
failure that fills the volume.

**verify:** notebook JSON parses; config cell carries the `parameters` tag; no reference to
`b0_baseline`; `CKPT`/`RUN_TAG` correct.

**done:** notebook committed, unrun.

### Task 2 — run it on an RTX 5090  🔶 BLOCKED ON GPU STOCK

**files:** none in-repo (produces run artifacts on the volume)

**Blocker recorded 2026-07-27.** EU-RO-1 has **no RTX 5090 stock** (the REST create returns a bare
500; the GraphQL `lowestPrice.stockStatus` is null). The network volume pins the DC, so the pod
cannot move. Available in EU-RO-1 at the time of writing: RTX 4090 $0.69, A100 80GB PCIe $1.39,
A100 SXM 80GB $1.49, RTX PRO 6000 WK $1.89.

**Why we do not just take another card.** The measured cross-GPU drift is ~0.5 % of answers
(199/200 on rung 13's identity gate), which moves `bucket_mean` by up to **±0.003** — the same
size as the difference this control exists to adjudicate (0.5699 vs 0.5667). A control measured on
a different card would arrive with a noise band as wide as its own effect and would not settle
anything.

**Decision (user, 2026-07-27): wait for 5090 stock.** The alternative that was on the table and
declined — re-evaluating all three ep3 checkpoints (06, 14, 15) together on one A100, ~2.5 h /
~$3.50, which would have made the three-way comparison fully within-machine — stays available if
the wait turns out to be long.

A background watcher polls `gpuTypes.lowestPrice(dataCenterId: "EU-RO-1")` every 5 minutes for up
to 24 h and exits the moment stock appears.

**action (once stock exists):**
1. Create a pod: RTX 5090, `cloudType: SECURE`, DC `EU-RO-1`, network volume `gf78k60nlt` at
   `/workspace`, image `runpod/pytorch:1.0.2-cu1281-torch280-ubuntu2404`, `containerDiskInGb: 60`,
   ports `8888/http` + `22/tcp`, env `HF_HOME=/workspace/hf_cache`, `HF_HUB_OFFLINE=1`,
   `TRANSFORMERS_OFFLINE=1`, `HF_HUB_ENABLE_HF_TRANSFER=0`, and the `PUBLIC_KEY` block copied from
   pod `m7s3xq835y9hd4` (the local `~/.ssh/id_rsa` is authorized in it — verified).
   ⚠️ Pass **only** the 5090 in `gpuTypeIds`: a fallback list would silently hand back a 4090 and
   defeat the entire point of waiting.
2. Run the R2 prep sanity: GPU name, `torch/transformers/swift/qwen_vl_utils/focus/papermill/decord`
   all importable, CUDA available, judge present, `df -h /workspace` has ≥ 40 GB free.
   **ENV_BAD or < 40 GB free → stop and report; do not improvise a fix.**
3. Sync `/workspace/repo` to this branch (`task/r3-rung16`) at the commit from Task 1.
   Do **not** wipe `runs/` — rung 06's adapters live there.
4. SMOKE first (`-p SMOKE False` omitted → 40 questions) to prove wiring, then the full run,
   detached via `nohup setsid` so it outlives the SSH socket.
5. **Optional, only if steps 1–4 are clean and disk allows:** re-evaluate rung 06's already-merged
   `checkpoint-1720` on this same card. It costs ~30 min and no merge, and it converts
   "ep3-on-5090 vs ep2-on-another-card" into a within-machine ladder. Skip it without hesitation if
   anything is tight — the primary result does not depend on it.
6. Pull `results.csv` + `stratified.json` down, **stop the pod.**

**verify:** `stratified.json` exists for `ep3_full`, 6252 rows in `results.csv`, all gates green in
the log, pod status EXITED.

**done:** artifacts local, pod stopped, cost reported.

### Task 3 — record the verdict

**files:** `experiments/06-vit-lora/RESULTS_epoch3.csv`, `experiments/06-vit-lora/README.md`,
`context/decisions/epoch-matched-control.md`, `context/NOW.md`

**action:** Write `RESULTS_epoch3.csv` from the canonical `stratified.json`. Fill the empty cell in
the decision note's table and replace its "the ep3 comparison is not decided" verdict with what was
measured, against the **pre-registered** read — not a read invented after seeing the number. Update
the rung 06 README ladder and the NOW.md entry.

**verify:** the note's table has no `never evaluated` cell; the verdict names which of the two
pre-registered branches fired.

**done:** committed.

## Out of scope (stated, not silently dropped)

- **Seed repeats.** The user explicitly declined them this session. The "no variance estimate"
  caveat therefore stands: a ±0.003 difference remains formally indistinguishable from noise, and
  this task cannot fix that.
- **Rung 02 ep3.** Not needed to settle 14/15.
- **The 14+15 fusion.** Blocked on this result by the decision note; not started here.
