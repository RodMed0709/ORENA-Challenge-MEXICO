---
phase: 01-foundation-eval-harness
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - pyproject.toml
  - requirements/harness.lock
  - requirements/train.in
  - requirements/serve.in
  - .gitignore
  - src/frame/__init__.py
  - scripts/verify_env.py
  - docs/COLLABORATION.md
  - docs/EXPERIMENTS.md
  - configs/example_run.yaml
  - experiments/runs/.gitkeep
  - README.md
autonomous: true
requirements: [FND-01, FND-02, FND-03, FND-04]
user_setup:
  - service: huggingface
    why: "Streaming FOCUS annotation parquet + (later) gated model/dataset access"
    env_vars:
      - name: HF_TOKEN
        source: "https://huggingface.co/settings/tokens (read scope)"
    dashboard_config:
      - task: "Verify orena-dkfz/heico-focus-vqa and orena-dkfz/lapchole-focus-vqa are reachable (login if gated)"
        location: "https://huggingface.co (huggingface-cli login on each machine)"

must_haves:
  truths:
    - "A fresh clone installs the Env C harness deps from a single lockfile and `import focus` succeeds"
    - "`transformers.__version__` starts with 4.57 and `focus.__version__` == 0.3.4"
    - "`.gitignore` blocks secrets, data, videos, frames, weights, temp clips, HF cache, and venvs"
    - "A teammate can read one doc and know the branch + versioned-YAML + committed-summary convention and each machine's FOCUS_ROOT_DIR"
  artifacts:
    - path: "pyproject.toml"
      provides: "Env C package metadata, pinned harness deps, pytest config"
      contains: "transformers==4.57"
    - path: "requirements/harness.lock"
      provides: "Fully reproducible Env C install"
      contains: "orena-focus"
    - path: "scripts/verify_env.py"
      provides: "FND-01/03 smoke: imports + version asserts"
      min_lines: 15
    - path: "docs/COLLABORATION.md"
      provides: "3-machine sync + secrets/data-never-committed convention (FND-02)"
      min_lines: 20
    - path: "docs/EXPERIMENTS.md"
      provides: "Experiment-tracking convention (FND-04)"
      min_lines: 20
  key_links:
    - from: "pyproject.toml"
      to: "requirements/harness.lock"
      via: "same pinned versions (orena-focus @ v0.3.4, transformers==4.57.*)"
      pattern: "transformers==4\\.57"
    - from: ".gitignore"
      to: ".secrets.env / data/ / *.safetensors"
      via: "ignore rules"
      pattern: "safetensors"
---

<objective>
Stand up the repository foundation the entire challenge builds on: a pinned, reproducible **Env C** (harness/EDA) that installs `orena-focus` @ git tag **v0.3.4** and `transformers==4.57.*` from a lockfile, the source-spec files for the two GPU envs (train/serve) that get locked later on RunPod, hardened `.gitignore` guards, and the two collaboration conventions (3-machine git sync + experiment tracking) every teammate follows.

Purpose: Everything downstream (data load, harness, engine, training) imports `focus` and runs in Env C. If the env is not pinned and reproducible from day one, numbers drift between the 3 machines and the whole "trustworthy scorer" premise collapses (CONSTITUTION §IV.8, §V).
Output: `pyproject.toml`, `requirements/*.lock|.in`, updated `.gitignore`, `src/frame/` package skeleton, `scripts/verify_env.py`, `docs/COLLABORATION.md`, `docs/EXPERIMENTS.md`, `configs/example_run.yaml`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@CONSTITUTION.md
@.planning/phases/01-foundation-eval-harness/01-RESEARCH.md

<constraints>
- All repo content in ENGLISH. NEVER add Claude as a git contributor — NO `Co-Authored-By` trailers, no AI in authorship (CLAUDE.md Process). Commit as the user.
- Python `>=3.10,<3.13`. `transformers==4.57.*` HARD pin even though this phase is GPU-free (the SDK judge needs the Qwen3 chat template; a lower 4.x breaks it — RESEARCH Pitfall 5).
- Pin `orena-focus` to git tag **v0.3.4** (`git+https://github.com/IMSY-DKFZ/orena-focus.git@v0.3.4`). PyPI currently ships only 0.1.1 (RESEARCH State-of-the-Art). If `pip index versions orena-focus` shows 0.3.4 on PyPI, either pin works — prefer the git tag for determinism.
- Two SEPARATE Python envs for train (ms-swift) vs serve (vLLM); do NOT merge them (torch/flash-attn pins conflict). Their lockfiles are produced later on RunPod/GPU hardware — this plan only writes their `.in` source specs. Env C is the one we fully lock and prove now.
- NEVER commit secrets/data/weights/videos/frames/temp clips (CONSTITUTION §V.3/§V.4).
</constraints>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Repo scaffold — pyproject.toml (Env C deps + pytest config), package skeleton, hardened .gitignore</name>
  <files>pyproject.toml, src/frame/__init__.py, .gitignore, README.md</files>
  <read_first>
    - .gitignore (current file — Spanish comments; you will replace with English and add guards)
    - README.md (current quickstart — you will append a Setup section)
    - .planning/phases/01-foundation-eval-harness/01-RESEARCH.md (§Standard Stack "Env C" table + Installation block for exact pins)
    - CONSTITUTION.md §V.3/§V.4 (never-commit list)
  </read_first>
  <action>
    Create `pyproject.toml` (PEP 621 + setuptools src-layout):
    - `[project]` name `orena-frame`, `requires-python = ">=3.10,<3.13"`, dependencies EXACTLY:
      `orena-focus @ git+https://github.com/IMSY-DKFZ/orena-focus.git@v0.3.4`,
      `transformers==4.57.*`, `datasets>=2.14`, `pandas>=2.0`, `numpy>=1.23`, `pyyaml>=6.0`.
    - `[project.optional-dependencies]` `dev = ["pytest>=8.0"]`.
    - `[tool.setuptools.packages.find]` `where = ["src"]`.
    - `[tool.pytest.ini_options]`: `testpaths = ["tests"]`, register markers `network: needs HF Hub network` and `gpu: needs a GPU`. Do NOT set `--strict-markers` (Plan B runs in the same wave and may use these markers before this file merges).
    Create `src/frame/__init__.py` with a module docstring and `__all__ = []` (subpackages `frame.data` and `frame.eval` are added by later plans).
    Replace `.gitignore` with an ENGLISH version that KEEPS every current guard (`.secrets.env`, `*.env`, `PROPUESTA-LEO.*`, `NEGOCIO/`, `literature/pdfs/`, `data/`, `datasets/`, `*.ckpt/*.safetensors/*.bin`, `checkpoints/`, `runs/`, `wandb/`, `.obsidian/`, `__pycache__/`, `*.pyc`) and ADDS: `*.mp4`, `*.avi`, `*.mov`, `*.mkv`, frames dirs (`**/frames/`), `.venv/`, `venv/`, `.pytest_cache/`, `*.egg-info/`, `build/`, `dist/`, HF cache note (`.cache/huggingface/`), and temp-clip pattern (`/tmp/*.mp4`). Keep `!.env.example`.
    Append a `## Setup (Env C — harness/EDA)` section to `README.md` documenting `python -m venv .venv`, activate, `pip install -e ".[dev]"` (or `pip install -r requirements/harness.lock`), and `python scripts/verify_env.py`.
  </action>
  <verify>
    <automated>python -c "import tomllib,sys; d=tomllib.load(open('pyproject.toml','rb')); deps=d['project']['dependencies']; assert any('transformers==4.57' in x for x in deps), deps; assert any('orena-focus' in x and 'v0.3.4' in x for x in deps), deps; print('pyproject OK')"</automated>
  </verify>
  <acceptance_criteria>
    - `python -c "import tomllib; ..."` above prints `pyproject OK` (exit 0): pins present and correct.
    - `grep -q "safetensors" .gitignore && grep -q ".secrets.env" .gitignore && grep -q "\.venv" .gitignore` all succeed.
    - `grep -qi "co-authored-by" .gitignore` returns nonzero (no AI-authorship artifacts introduced anywhere).
    - `test -f src/frame/__init__.py` succeeds.
    - No non-English prose remains in `.gitignore` (manual eyeball: comments are English).
  </acceptance_criteria>
  <done>`pyproject.toml` declares the exact Env C pins + pytest markers; `.gitignore` (English) blocks all secrets/data/weights/clips; `src/frame/` is an importable package; README documents setup.</done>
</task>

<task type="auto">
  <name>Task 2: Two-env dependency specs + fully-locked Env C + verify_env smoke (FND-01, FND-03)</name>
  <files>requirements/train.in, requirements/serve.in, requirements/harness.lock, scripts/verify_env.py</files>
  <read_first>
    - pyproject.toml (from Task 1 — source of the Env C pin set)
    - .planning/phases/01-foundation-eval-harness/01-RESEARCH.md (§Two-env lockfiles table: exact train/serve pin sets)
    - .planning/research/STACK.md (§Pinned dependency set — train vs serve)
  </read_first>
  <action>
    Create `requirements/train.in` (Env A source spec, locked LATER on RunPod — add a header comment saying so): `transformers==4.57.*`, `torch>=2.5`, `qwen-vl-utils>=0.0.14`, `ms-swift>=4.2`, `accelerate>=1.0`, `bitsandbytes>=0.45`, `deepspeed>=0.15`, `flash-attn>=2.6`, `peft>=0.13`, `orena-focus @ git+https://github.com/IMSY-DKFZ/orena-focus.git@v0.3.4`.
    Create `requirements/serve.in` (Env B source spec, locked LATER on the serving box): `vllm>=0.11`, `transformers==4.57.*`, `qwen-vl-utils>=0.0.14`, `decord>=0.6`, `opencv-python>=4.8`, `orena-focus @ git+https://github.com/IMSY-DKFZ/orena-focus.git@v0.3.4`.
    Produce `requirements/harness.lock` — the FULLY PINNED Env C install. Create a clean venv, `pip install -e .`, then `pip freeze` filtered to the harness closure into `requirements/harness.lock` (must contain exact `==` pins for `orena-focus` (or its VCS URL @ v0.3.4), `transformers`, `datasets`, `pandas`, `numpy`, `pyyaml`, and their transitive deps). Prepend a header comment: generated on Env C, Python version used, regeneration command.
    Create `scripts/verify_env.py`: import `focus`, `transformers`, `datasets`, `pandas`, `numpy`; assert `transformers.__version__.startswith("4.57")`; assert `focus.__version__ == "0.3.4"`; print all versions; exit 0 on success, nonzero with a clear message on any failure. This is the FND-01 automated smoke (the full example-eval-on-real-data run is a manual smoke — see success_criteria).
  </action>
  <verify>
    <automated>pip install -r requirements/harness.lock && python scripts/verify_env.py</automated>
  </verify>
  <acceptance_criteria>
    - `pip install -r requirements/harness.lock` exits 0 in a clean venv.
    - `python scripts/verify_env.py` exits 0 and prints a line containing `4.57` and a line containing `0.3.4`.
    - `grep -q "transformers==4.57" requirements/harness.lock` succeeds; `grep -q "orena-focus" requirements/harness.lock` succeeds.
    - `grep -q "ms-swift" requirements/train.in && grep -q "vllm" requirements/serve.in` succeeds.
    - `python -c "import focus; from focus import Evaluator, FocusDataset, load_responses, Track, DatasetSplit; print('focus imports OK')"` exits 0.
  </acceptance_criteria>
  <done>Env C installs reproducibly from `harness.lock` and `verify_env.py` proves the version floors; train/serve `.in` specs exist for later locking on GPU hardware.</done>
</task>

<task type="auto">
  <name>Task 3: Collaboration + experiment-tracking conventions + run-config template (FND-02, FND-04)</name>
  <files>docs/COLLABORATION.md, docs/EXPERIMENTS.md, configs/example_run.yaml, experiments/runs/.gitkeep</files>
  <read_first>
    - CONSTITUTION.md §V (git/sync rules) + §IV.8 (reproducibility) + §I.1 (repo is an SDK, not a submission template)
    - .planning/research/ARCHITECTURE.md (§"One experiment = one branch + one YAML"; §"Git vs Outside Git" table)
    - .planning/phases/01-foundation-eval-harness/01-RESEARCH.md (FND-02/FND-04 rows; Runtime State Inventory — FOCUS_ROOT_DIR / HF_TOKEN)
  </read_first>
  <action>
    Create `docs/COLLABORATION.md` (English): repo `RodMed0709/ORENA-Challenge-MEXICO`; 3 machines (local, RunPod, teammate). Document: `main` always runnable; work on `feat/` `exp/` `fix/` branches; `git pull` before, `push` after; secrets in `.secrets.env` (gitignored) — list each secret (`HF_TOKEN`, `FOCUS_ROOT_DIR`); each machine sets its OWN `FOCUS_ROOT_DIR` pointing at a LOCAL data root that is never committed (data/weights live on RunPod/HF, per §V.4); `huggingface-cli login` per machine; the never-commit checklist mirroring `.gitignore`.
    Create `docs/EXPERIMENTS.md` (English): the convention `experiments/runs/<id>/{config.yaml, summary.csv, checkpoint_uri.txt}` (tiny, committed; weights themselves on HF/RunPod). One experiment = one branch + one versioned YAML. Specify the required config.yaml fields: `seed`, `dataset`, `split`, `track` (always `frame`), `revision` (HF dataset tag or null), `judge_mode` (`no-judge|mock|real`), `model_id`, `split_hash` (placeholder until Phase 3), plus a free `notes` field. Explain that `summary.csv` is the `summary_df` the harness writes and `checkpoint_uri.txt` records the HF/RunPod weight location.
    Create `configs/example_run.yaml` — a filled template instance of the above field set (seed: 42, dataset: heico, split: test, track: frame, revision: null, judge_mode: no-judge, model_id: null, split_hash: null, notes: "Phase 1 harness smoke").
    Create `experiments/runs/.gitkeep` (empty) so the tracked dir exists.
  </action>
  <verify>
    <automated>python -c "import yaml; d=yaml.safe_load(open('configs/example_run.yaml')); req={'seed','dataset','split','track','revision','judge_mode','model_id','split_hash'}; missing=req-set(d); assert not missing, missing; assert d['track']=='frame' and d['seed']==42; print('config template OK')"</automated>
  </verify>
  <acceptance_criteria>
    - The python/yaml check above prints `config template OK` (exit 0): all required fields present, `track==frame`, `seed==42`.
    - `test -f docs/COLLABORATION.md && test -f docs/EXPERIMENTS.md && test -f experiments/runs/.gitkeep` succeeds.
    - `grep -q "FOCUS_ROOT_DIR" docs/COLLABORATION.md && grep -q "main always runnable" docs/COLLABORATION.md` (or equivalent wording) — sync + per-machine data-root convention documented.
    - `grep -q "one branch" docs/EXPERIMENTS.md` (case-insensitive) and `grep -q "config.yaml" docs/EXPERIMENTS.md` — experiment convention documented.
    - Both docs are English prose (manual eyeball).
  </acceptance_criteria>
  <done>Any teammate can read two docs and correctly sync the repo (no secrets/data committed) and record an experiment; a valid `config.yaml` template exists for the harness to consume.</done>
</task>

</tasks>

<verification>
- Clean-venv reproducibility: `pip install -r requirements/harness.lock && python scripts/verify_env.py` exits 0 (FND-03 + FND-01 import path).
- `python -c "from focus import Evaluator, FocusDataset, load_responses"` exits 0.
- `.gitignore` blocks secrets/data/weights/clips; `git status` shows no secret/data files staged.
- Config template validates; both convention docs exist and are English.
</verification>

<success_criteria>
- FND-01: `focus` + `transformers==4.57.*` install and import on one machine via the lockfile; `verify_env.py` green. (Full manual smoke — run the SDK's `examples/evaluation.py` against a real split + responses file on a machine with `FOCUS_ROOT_DIR` set — is the human FND-01 sign-off before `/gsd:verify-work`.)
- FND-02: `.gitignore` + `docs/COLLABORATION.md` guarantee secrets/data never committed and document 3-machine clone/pull/push + per-machine `FOCUS_ROOT_DIR`.
- FND-03: `requirements/harness.lock` reproduces Env C; `train.in`/`serve.in` capture the two GPU-env specs for later locking (NOT merged).
- FND-04: `docs/EXPERIMENTS.md` + `configs/example_run.yaml` define the branch + versioned-YAML + committed-summary convention usable by all 3 members.
</success_criteria>

<output>
After completion, create `.planning/phases/01-foundation-eval-harness/01-01-SUMMARY.md` using the summary template. Note the exact Python version used to generate `harness.lock` and whether `orena-focus` 0.3.4 was found on PyPI or installed from the git tag.
</output>
