---
phase: 01-foundation-eval-harness
plan: 04
type: execute
wave: 3
depends_on: ["01-03"]
files_modified:
  - src/frame/eval/reporting.py
  - src/frame/eval/harness.py
  - scripts/evaluate.py
  - tests/test_buckets.py
autonomous: true
requirements: [EVAL-03]

must_haves:
  truths:
    - "The harness reports accuracy per capability-group × {ID, OOD} bucket, not just the headline mean"
    - "The OOD (ood=True) bucket path is exercised by a synthetic fixture (public data is all ood=False)"
    - "A duplicate qID in the response set raises ValueError and aborts the run (never silently averaged)"
    - "The bucket report is written as a committed-style artifact and printed by the CLI"
  artifacts:
    - path: "src/frame/eval/reporting.py"
      provides: "format_bucket_report(buckets_df)->markdown + write_bucket_report(); check_unique_qids()"
      min_lines: 30
    - path: "tests/test_buckets.py"
      provides: "synthetic ID+OOD buckets golden + duplicate-qID abort"
      min_lines: 30
  key_links:
    - from: "src/frame/eval/reporting.py"
      to: "Evaluator.pre_evaluation_score buckets_df (group,ood,accuracy,count)"
      via: "formats every group×{ID,OOD} row into the report"
      pattern: "ood"
    - from: "scripts/evaluate.py"
      to: "src/frame/eval/reporting.py::format_bucket_report"
      via: "CLI prints/writes the bucket breakdown"
      pattern: "bucket"
---

<objective>
Complete the harness's honesty guarantee: report accuracy per **capability-group × {ID, OOD}** bucket (the metric is an unweighted mean over these ≤10 buckets — being terrible in one bucket sinks the score), exercise the OOD path with a SYNTHETIC `ood=True` fixture (public data is 100% `ood=False`, so this branch is otherwise never tested and a bug ships silently to Phase 4), and prove the duplicate-qID guard aborts the whole run.

Purpose: EVAL-03 is where over-fitting to cholecystectomy (ID) gets caught — OOD weighs equally (CONSTITUTION §III/§IV.2). A per-bucket report that never populates the OOD half looks green but is blind. The duplicate-qID abort is a hard SDK gate (CONSTITUTION §I.4-bis #4) we must surface, not suppress.
Output: `src/frame/eval/reporting.py`, a `check_unique_qids` guard in the harness, CLI wiring, and `tests/test_buckets.py`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@CONSTITUTION.md
@.planning/phases/01-foundation-eval-harness/01-RESEARCH.md
@.planning/phases/01-foundation-eval-harness/01-03-SUMMARY.md

<interfaces>
<!-- Exact SDK contracts (v0.3.4). The harness surface comes from Plan 01-03 (see 01-03-SUMMARY). -->

Evaluator.pre_evaluation_score(results_df) -> (score, buckets_df)
  - buckets_df columns: group, ood, accuracy, count  (one row per POPULATED group×{False,True} bucket).
  - Iterates sorted groups × ood in (False, True); empty buckets are SKIPPED (so ≤10 rows).
  - score = buckets_df["accuracy"].mean()  (EXACT; e.g. object_recognition/ID=0.5 + aggregation/OOD=1.0 -> 0.75).

Evaluator.run(...): raises ValueError("Duplicate response for qID=...") if two Responses share a qID
  → aborts the ENTIRE run (no partial score). Do NOT catch-and-continue.

From Plan 01-03 (tests/conftest.py): make_request, make_reference(..., ood: bool=False), make_response.
  Reference.ood defaults False; set ood=True in the fixture to build the OOD half.
From Plan 01-03 (src/frame/eval/harness.py): score(references, requests, responses, judges=None,
  judge_mode=..., output_dir=None) -> HarnessResult(pre_score, buckets_df, results_df, summary_df, judge_mode).

focus.Capability: leaf.group.value gives the bucket group key (e.g. OBJECT_IDENTIFICATION -> "object_recognition",
  OBJECT_AGGREGATION -> "aggregation"). Capability.groups() -> the 5 groups (expected 10 buckets = 2×5).
</interfaces>

<constraints>
- English content only. Commit as the user (NO Co-Authored-By / no AI authorship).
- NEVER recompute the bucket mean — read `buckets_df` from `pre_evaluation_score`; reporting only FORMATS it.
- The synthetic fixture is REQUIRED: public `Reference.ood` is always False, so without ood=True rows the OOD branch is untested (RESEARCH Pitfall 2). Use ≥2 groups and both ood values.
- Duplicate-qID must raise (mirror SDK `test_duplicate_response_raises`); the harness `check_unique_qids` is a friendly PRE-check that raises the SAME clear error — it must NOT swallow the SDK's ValueError (RESEARCH Pitfall 3).
- Reuse conftest fixtures from Plan 01-03 (do not redefine make_reference).
</constraints>
</context>

<tasks>

<task type="auto">
  <name>Task 1: reporting.py (group×{ID,OOD} report) + check_unique_qids guard + CLI wiring</name>
  <files>src/frame/eval/reporting.py, src/frame/eval/harness.py, scripts/evaluate.py</files>
  <read_first>
    - src/frame/eval/reporting.py (target)
    - src/frame/eval/harness.py (from 01-03 — add check_unique_qids; HarnessResult already carries buckets_df)
    - scripts/evaluate.py (from 01-03 — add bucket-report print/write)
    - .planning/phases/01-foundation-eval-harness/01-RESEARCH.md (EVAL-03 row; Pattern 1 buckets_df)
    - The <interfaces> block above (buckets_df schema + Capability.groups())
  </read_first>
  <action>
    Create `src/frame/eval/reporting.py`:
    - `def format_bucket_report(buckets_df) -> str`: render a markdown table with columns Group | Distribution (ID/OOD from the `ood` bool) | Accuracy | Count, one row per bucket in buckets_df, plus a footer line "Buckets populated: N/10" using `2*len(Capability.groups())` as the denominator. If a group is missing its OOD (or ID) half, that is expected on public data — do NOT fabricate the missing row; the footer count communicates coverage.
    - `def write_bucket_report(buckets_df, path) -> None`: write the markdown to `path`.
    - `def check_unique_qids(responses) -> None`: raise `ValueError(f"Duplicate response for qID={qid!r}.")` on the first repeated qID (same message shape as the SDK) — a friendly PRE-check callers can run before `run()`. Does NOT replace the SDK guard.
    In `src/frame/eval/harness.py`: call `check_unique_qids(responses)` at the top of `score()` (before building the Evaluator) so the abort is surfaced early with a clear message; let the SDK's own ValueError still stand as the backstop (do not try/except-swallow it).
    In `scripts/evaluate.py`: after scoring, print `format_bucket_report(result.buckets_df)`; if `--output-dir` is set, also `write_bucket_report(result.buckets_df, output_dir/"buckets.md")`.
  </action>
  <verify>
    <automated>python -c "from frame.eval.reporting import format_bucket_report, check_unique_qids, write_bucket_report; print('reporting imports OK')" && python -m pytest -q tests/ -m "not network and not gpu" -k "harness or responses or judge"</automated>
  </verify>
  <acceptance_criteria>
    - `python -c "from frame.eval.reporting import format_bucket_report, write_bucket_report, check_unique_qids; import pandas as pd; df=pd.DataFrame([{'group':'aggregation','ood':True,'accuracy':1.0,'count':2},{'group':'object_recognition','ood':False,'accuracy':0.5,'count':2}]); s=format_bucket_report(df); assert 'aggregation' in s and 'OOD' in s and 'ID' in s and '2' in s; print('report OK')"` prints `report OK` (exit 0).
    - The duplicate-qID guard raises: `python -m pytest -q tests/test_buckets.py::test_duplicate_qid_aborts` exits 0 (asserts ValueError mentioning the dup qID).
    - `grep -q "check_unique_qids" src/frame/eval/harness.py` succeeds (wired into score()).
    - `grep -q "format_bucket_report" scripts/evaluate.py` succeeds (CLI prints the bucket breakdown).
    - The previously-green 01-03 tests still pass: `python -m pytest -q tests/ -m "not network and not gpu" -k "harness or responses or judge"` exits 0 (no regression from adding the pre-check).
  </acceptance_criteria>
  <done>`reporting.py` renders every group×{ID,OOD} bucket with a coverage footer; `check_unique_qids` gives an early clear abort without suppressing the SDK guard; the CLI prints/writes the bucket report.</done>
</task>

<task type="auto">
  <name>Task 2: EVAL-03 tests — synthetic ID+OOD bucket golden + duplicate-qID abort</name>
  <files>tests/test_buckets.py</files>
  <read_first>
    - tests/test_buckets.py (target)
    - tests/conftest.py (from 01-03 — make_request/make_reference(ood=)/make_response)
    - src/frame/eval/harness.py (score) + src/frame/eval/reporting.py (format_bucket_report)
    - .planning/phases/01-foundation-eval-harness/01-RESEARCH.md (Code Examples "Golden reproduction test"; Pitfalls 2 & 3)
    - The <interfaces> block above (buckets_df schema; 0.75 golden example)
  </read_first>
  <action>
    Create `tests/test_buckets.py` (OFFLINE, GPU-free), reusing conftest fixtures:
    - `test_id_ood_buckets` (EVAL-03 core): build 4 exact-match `number` questions across TWO groups AND both distributions —
      group object_recognition, ood=False: one correct + one wrong → ID bucket accuracy 0.5;
      group aggregation, ood=True: two correct → OOD bucket accuracy 1.0.
      Run `score(judges=[])`. Assert `len(result.buckets_df) == 2`; assert a row `(group=='object_recognition', ood==False, accuracy==0.5)` and a row `(group=='aggregation', ood==True, accuracy==1.0)` exist (EXACT); assert `result.pre_score == 0.75` (`==`, not approx). Assert `format_bucket_report(result.buckets_df)` contains both "ID" and "OOD".
    - `test_ood_bucket_is_populated`: assert at least one `buckets_df.ood == True` row exists in the above (proves the OOD branch is actually exercised — the whole point of the synthetic fixture).
    - `test_duplicate_qid_aborts` (EVAL-03 abort): two responses with the SAME qID → `with pytest.raises(ValueError): score(references, requests, [resp_dup1, resp_dup2], judges=[])`. Assert the error message mentions the duplicate qID. (This covers both the harness pre-check and, if bypassed, the SDK guard.)
  </action>
  <verify>
    <automated>python -m pytest -q tests/test_buckets.py</automated>
  </verify>
  <acceptance_criteria>
    - `python -m pytest -q tests/test_buckets.py` exits 0.
    - `grep -q "ood=True" tests/test_buckets.py` succeeds (synthetic OOD fixture present — RESEARCH Pitfall 2).
    - `grep -q "== 0.75" tests/test_buckets.py` succeeds (exact golden, not approx).
    - `grep -q "pytest.raises(ValueError)" tests/test_buckets.py` succeeds (duplicate-qID abort asserted).
    - Full offline suite green: `python -m pytest -q tests/ -m "not network and not gpu"` exits 0.
  </acceptance_criteria>
  <done>EVAL-03 is proven: the ID/OOD × group breakdown is exact on a synthetic fixture that populates the OOD half, and a duplicate qID aborts with a clear ValueError.</done>
</task>

</tasks>

<verification>
- `python -m pytest -q tests/ -m "not network and not gpu"` exits 0 (all Phase 1 unit tests including buckets + dup-qID).
- `format_bucket_report` renders both ID and OOD rows; harness runs `check_unique_qids` without swallowing the SDK ValueError.
- CLI prints the group×{ID,OOD} bucket table.
</verification>

<success_criteria>
- EVAL-03: the harness reports accuracy per capability-group × {ID, OOD} bucket (not just the mean); the OOD path is exercised by a synthetic `ood=True` fixture; a duplicate qID aborts the entire run with a clear error.
</success_criteria>

<output>
After completion, create `.planning/phases/01-foundation-eval-harness/01-04-SUMMARY.md`. Confirm the OOD bucket golden reproduced exactly (0.75) and that the duplicate-qID abort fires. Note that real ood=True labels arrive only with the private test split at Jul 15 (RESEARCH Open Question 3) — the synthetic fixture is the standing guard until then.
</output>
