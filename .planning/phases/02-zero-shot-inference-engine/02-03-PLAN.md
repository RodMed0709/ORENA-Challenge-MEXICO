---
phase: 02-zero-shot-inference-engine
plan: 03
type: execute
wave: 3
depends_on: ["02-02", "01-03", "01-04"]
files_modified:
  - scripts/run_baseline.py
  - tests/test_run_baseline.py
  - reports/baseline_heico_frame.json
autonomous: false
requirements: [MODEL-03]

must_haves:
  truths:
    - "run_baseline.py runs the engine over FocusVideoDataset(heico, TEST, Track.FRAME), collects Response(qID, content, latency), and writes a UNIQUE-qID responses.json via save_items"
    - "responses.json flows through the Phase-1 harness (score, Track.FRAME) to produce a pre_evaluation_score with a per-group x {ID,OOD} bucket table"
    - "A committed reports/baseline_heico_frame.json records the SCORE labeled with track=FRAME, model-id, and judge-mode — before Jul 15"
  artifacts:
    - path: "scripts/run_baseline.py"
      provides: "MODEL-03 driver: FocusVideoDataset(FRAME) -> engine loop -> save_items -> Phase-1 harness -> labeled report JSON"
      min_lines: 45
    - path: "tests/test_run_baseline.py"
      provides: "CPU wiring test: StubEngine over make_video_sample list -> unique-qID responses.json -> harness score(judges=[]) numeric"
      min_lines: 25
    - path: "reports/baseline_heico_frame.json"
      provides: "The committed zero-shot number (GPU run on RunPod) with track/model/judge-mode labels — the always-valid floor"
      contains: "pre_evaluation_score"
  key_links:
    - from: "scripts/run_baseline.py"
      to: "focus.data.video_dataset.FocusVideoDataset (Track.FRAME)"
      via: "builds the FRAME clip stream (NOT Track.SEGMENT from the example)"
      pattern: "Track.FRAME"
    - from: "scripts/run_baseline.py"
      to: "focus.save_items"
      via: "serializes Responses to the canonical responses.json seam"
      pattern: "save_items"
    - from: "scripts/run_baseline.py"
      to: "frame.eval.harness.score"
      via: "scores responses.json through the Phase-1 harness (never recomputes the metric)"
      pattern: "score"
---

<objective>
Produce the Jul-15 deliverable: a committed zero-shot `pre_evaluation_score` for the open backbone on HeiCo, measured through the Phase-1 harness (MODEL-03) — the honest baseline and the always-valid submission floor. `scripts/run_baseline.py` wires the pieces the earlier plans built: `FocusVideoDataset(FocusDataset("heico", TEST, Track.FRAME))` → `QwenInferenceEngine.predict` loop → `Response(qID, content, latency)` → `save_items(responses.json)` → the Phase-1 `harness.score` (Track.FRAME) → a labeled `reports/baseline_heico_frame.json`. The wiring is CPU-tested with the StubEngine over synthetic samples; the real number REQUIRES GPU + downloaded HeiCo videos + the judge and is run once on RunPod (the plan's checkpoint).

Purpose: This artifact IS the floor (CONSTITUTION §IV.1) — a packaged >0 submission must be achievable from it, so it must carry unique qIDs, <=300-char adversarial-clean content, and the exact labels (track=FRAME, model-id, judge-mode) that make the number diagnosable against the leaderboard on Jul 15. The driver hard-codes `Track.FRAME` (the example ships SEGMENT → the wrong 15s gate) and NEVER recomputes the metric — it extracts from the SDK via the Phase-1 harness (RESEARCH Pitfall 5, Don't-Hand-Roll).
Output: `scripts/run_baseline.py`, `tests/test_run_baseline.py`, and the committed `reports/baseline_heico_frame.json`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@CONSTITUTION.md
@.planning/phases/02-zero-shot-inference-engine/02-RESEARCH.md
@.planning/phases/02-zero-shot-inference-engine/02-VALIDATION.md

<interfaces>
<!-- Exact contracts. orena-focus v0.3.4 + Phase-1/02-01/02-02 artifacts. Use directly — do NOT re-explore. -->

# From Phase 1 (01-03/01-04):
from frame.eval.harness import score, resolve_judges, HarnessResult   # score(references, requests, responses, judges=..., judge_mode=...)
  # HarnessResult: .pre_score (float), .buckets_df (group x {ID,OOD}), .results_df, .summary_df, .judge_mode
  # resolve_judges("no-judge")->[]  "mock"->[MockJudge]  "real"->None (SDK lazy TransformersJudge Qwen/Qwen3.5-4B)

# From 02-02 (this phase):
from frame.engine.engine import QwenInferenceEngine   # .load(); .predict(sample)->str (unlinks clip internally)

# SDK (v0.3.4):
from focus import FocusConfig, set_config, save_items      # save_items(list[Response], path) -> canonical responses.json
from focus.data.base_dataset import FocusDataset           # FocusDataset("heico", DatasetSplit.TEST, Track.FRAME)
from focus.data.video_dataset import FocusVideoDataset      # FocusVideoDataset(base, stride=1) -> yields VideoSample (temp clip)
from focus.data.data_models import Response                 # Response(qID:str, content:str, latency:float)
from focus.enums import DatasetSplit, Track
# set_config(FocusConfig(root_dir=os.environ["FOCUS_ROOT_DIR"]))  # videos must be downloaded (Phase-1 dep); FocusConfig raises if root_dir missing.

# tests/conftest.py (from 02-01): StubEngine, make_video_sample(tmp_path, question, qID)
</interfaces>

<constraints>
- English content only. Commit as the user — NO `Co-Authored-By`, no AI authorship (CONSTITUTION §V; CLAUDE.md).
- Hard-code `Track.FRAME` + `DatasetSplit.TEST` (RESEARCH Pitfall 5 — the example ships Track.SEGMENT / Qwen3-VL-4B). Label the number with track + model-id + judge-mode.
- NEVER recompute the metric — call the Phase-1 `harness.score` and read `pre_score`/`buckets_df` (Don't-Hand-Roll; the harness reproduces the SDK exactly).
- Unique qIDs across the full response set — a duplicate aborts the whole eval (CONSTITUTION §I.4-bis #4). Assert uniqueness before writing.
- Never commit videos, temp clips, weights, or the full responses.json-with-data — only the tiny labeled `reports/baseline_heico_frame.json` (+ optionally summary.csv) (CONSTITUTION §V.3/§V.4).
- The REAL run needs GPU + downloaded HeiCo videos + the Qwen3.5-4B judge — it is a checkpoint (RunPod). The `judges=[]` exact-match-only number is a fast lower-bound sanity value; record which mode produced the committed number (RESEARCH Pitfall 6).
- Factor the driver so the inference loop + scoring + report-writing are importable functions (CPU-testable with StubEngine) — the CLI `main()` only wires them.
</constraints>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: run_baseline.py driver + CPU wiring test (MODEL-03 wiring)</name>
  <files>scripts/run_baseline.py, tests/test_run_baseline.py</files>
  <read_first>
    - scripts/run_baseline.py (target)
    - examples/inference.py in the cloned SDK (the main loop we adapt — path in planning_context <files_to_read>)
    - src/frame/engine/engine.py (from 02-02 — QwenInferenceEngine)
    - src/frame/eval/harness.py (from 01-03 — score, resolve_judges, HarnessResult)
    - tests/conftest.py (from 02-01 — StubEngine, make_video_sample)
    - .planning/phases/02-zero-shot-inference-engine/02-RESEARCH.md (Pattern 3 baseline driver; Pitfalls 5,6)
    - The <interfaces> block above
  </read_first>
  <behavior>
    - run_inference(dataset_iterable, engine) -> list[Response]: one Response per sample, latency measured with time.perf_counter (float), content = engine.predict(sample) (str), qID = sample.request.qID; the loop closes each sample's temp clip (engine.predict unlinks it).
    - The produced Responses have UNIQUE qIDs (assert before writing); save_items(responses, path) writes a responses.json that focus.load_responses reads back.
    - score_responses(references, requests, responses, judge_mode) -> HarnessResult: delegates to harness.score with Track.FRAME (via the harness) and returns a numeric .pre_score.
    - write_report(result, model_id, track, judge_mode, path) -> dict: writes a small JSON containing pre_evaluation_score, track="FRAME", model_id, judge_mode, and per-bucket accuracies.
    - CPU wiring (StubEngine + N make_video_sample items): run_inference -> unique-qID Responses -> save_items -> load_responses -> harness.score(judges=[]) yields a float pre_score; each sample's temp clip is unlinked.
  </behavior>
  <action>
    Create `scripts/run_baseline.py`:
    - `def run_inference(samples, engine) -> list[Response]`: loop `for sample in samples:` `t0=time.perf_counter(); content=engine.predict(sample); responses.append(Response(qID=sample.request.qID, content=content, latency=time.perf_counter()-t0))`. After the loop, assert `len({r.qID for r in responses}) == len(responses)` (unique qIDs, §I.4-bis #4) — raise a clear error otherwise.
    - `def score_responses(references, requests, responses, judge_mode="real") -> HarnessResult`: `return score(references=references, requests=requests, responses=responses, judges=resolve_judges(judge_mode), judge_mode=judge_mode)` (harness hard-codes Track.FRAME).
    - `def write_report(result, model_id, track, judge_mode, path) -> dict`: build `{"pre_evaluation_score": result.pre_score, "track": track, "model_id": model_id, "judge_mode": judge_mode, "buckets": result.buckets_df.to_dict(orient="records"), "n_responses": int(...)}`; `json.dump` to `path` (create `reports/` if missing); return the dict.
    - `def main()` (argparse): `--dataset heico` (default), `--split test`, `--model-id Qwen/Qwen3-VL-8B-Instruct`, `--judge-mode {real,no-judge,mock}` (default real), `--num-eval` (int|None), `--root-dir` (default `os.environ["FOCUS_ROOT_DIR"]`), `--out reports/baseline_heico_frame.json`. Body: `set_config(FocusConfig(root_dir=...))`; `base = FocusDataset(args.dataset, DatasetSplit.TEST, Track.FRAME)` (HARD FRAME); `vds = FocusVideoDataset(base, stride=1)`; optional `itertools.islice` for `--num-eval`; `engine = QwenInferenceEngine(args.model_id); engine.load()`; `responses = run_inference(vds, engine)`; `result = score_responses(base.references, base.requests, responses, args.judge_mode)`; `write_report(result, args.model_id, "FRAME", args.judge_mode, args.out)`; print the SCORE + judge-mode label. Optionally `save_items(responses, <run-dir>/responses.json)` under a gitignored run dir.
    - Guard `main()` behind `if __name__ == "__main__":` so importing the functions for tests loads no dataset / GPU.
    Create `tests/test_run_baseline.py` (CPU, no GPU, no network — `-m "not gpu and not network"`):
    - `test_run_inference_unique_qids`: build 4 `make_video_sample(tmp_path, qID=f"q{i}")`; `resp = run_inference(samples, StubEngine())`; assert `len(resp)==4`, all `isinstance(r.latency, float)`, and qIDs unique; assert each `sample.video_path` no longer exists (StubEngine unlinked).
    - `test_responses_scoreable`: `save_items(resp, tmp/responses.json)`; `from focus import load_responses; back = load_responses(...)`; build matching references/requests (reuse make_reference with exact-match `number` format so no judge needed); `result = score_responses(refs, reqs, back, judge_mode="no-judge")`; assert `isinstance(result.pre_score, float)`.
    - `test_hard_frame_and_no_metric_recompute`: `grep`-style asserts in code — read `scripts/run_baseline.py` text; assert `"Track.FRAME"` present and `"Track.SEGMENT"` absent; assert it calls `score(`/`resolve_judges` (delegates) and contains no manual `.mean()` over buckets.
    - `test_write_report_labels`: `write_report(fake_result, "Qwen/Qwen3-VL-8B-Instruct", "FRAME", "no-judge", tmp/out.json)`; reload JSON; assert keys `pre_evaluation_score`, `track=="FRAME"`, `model_id`, `judge_mode` present.
  </action>
  <verify>
    <automated>python -m pytest -q tests/test_run_baseline.py -m "not gpu and not network"</automated>
  </verify>
  <acceptance_criteria>
    - `python -m pytest -q tests/test_run_baseline.py -m "not gpu and not network"` exits 0 (unique-qid, scoreable, hard-FRAME, report-labels all green with StubEngine + judges=[]).
    - Hard FRAME: `grep -q "Track.FRAME" scripts/run_baseline.py` succeeds AND `grep -q "Track.SEGMENT" scripts/run_baseline.py` returns nonzero (SEGMENT NOT copied from the example).
    - No metric recompute: `grep -q "resolve_judges" scripts/run_baseline.py` and `grep -q "score(" scripts/run_baseline.py` succeed; `grep -qE "\\.mean\\(\\)" scripts/run_baseline.py` returns nonzero (delegates to the harness).
    - Unique-qID guard present: `grep -q "qID" scripts/run_baseline.py` and the code asserts set-length equality (the wiring test proves it).
    - `python scripts/run_baseline.py --help` exits 0 and lists `--dataset`, `--judge-mode`, `--model-id`, `--num-eval`.
    - Importing the module runs no dataset/GPU: `python -c "import importlib.util,sys; spec=importlib.util.spec_from_file_location('rb','scripts/run_baseline.py'); m=importlib.util.module_from_spec(spec); spec.loader.exec_module(m); assert hasattr(m,'run_inference') and hasattr(m,'score_responses')"` exits 0.
  </acceptance_criteria>
  <done>run_baseline.py wires engine→Response→save_items→Phase-1 harness with hard-coded Track.FRAME, a unique-qID guard, and a labeled report writer; the whole pipeline is proven CPU-side with StubEngine + judges=[] (no GPU, no HeiCo videos). MODEL-03 wiring complete.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2 (GPU/RunPod checkpoint): run the real HeiCo FRAME zero-shot baseline and commit the labeled number</name>
  <files>reports/baseline_heico_frame.json</files>
  <action>
    This step RUNS on a GPU box (RunPod) — it is planned locally but cannot execute here (no GPU, no HeiCo videos, no judge). Claude prepares nothing further to write; the human runs the CPU-verified driver on RunPod, then commits ONLY the tiny labeled report. See how-to-verify for the exact commands.
  </action>
  <what-built>
    The full zero-shot baseline driver `scripts/run_baseline.py` (CPU-verified end-to-end with StubEngine + judges=[]) plus the GPU-marked real-engine smoke test from 02-02. Everything needed to produce the committed HeiCo FRAME zero-shot number is ready; it only needs a GPU box with the model + judge + downloaded HeiCo videos.
  </what-built>
  <how-to-verify>
    On RunPod (serve/inference env: transformers==4.57.*, qwen-vl-utils>=0.0.14, orena-focus v0.3.4, decord/opencv, accelerate), with `FOCUS_ROOT_DIR` pointing at downloaded HeiCo videos:
    1. Sanity: `pytest -q -m "not gpu" tests/` green, then `pytest -q -m gpu tests/test_real_engine.py` — one real predict returns a non-error str; note per-call latency (< 5s on L40S is the target; over-4s is a Phase-6 flag).
    2. Full run: `python scripts/run_baseline.py --dataset heico --model-id Qwen/Qwen3-VL-8B-Instruct --judge-mode real --out reports/baseline_heico_frame.json`
       (optionally also `--judge-mode no-judge` for the fast exact-match-only lower bound).
    3. Confirm `reports/baseline_heico_frame.json` contains `pre_evaluation_score` with `track=="FRAME"`, the model-id, and the judge-mode used; the score is > 0.
    4. Commit ONLY `reports/baseline_heico_frame.json` (+ optionally `summary.csv`) — never the videos, temp clips, weights, or the full responses.json-with-data. Do this BEFORE Jul 15.
  </how-to-verify>
  <resume-signal>Reply "approved" with the committed pre_evaluation_score + judge-mode, or describe any run failure (device error, >5s latency, missing videos, judge OOM) to diagnose.</resume-signal>
</task>

</tasks>

<verification>
- CPU: `python -m pytest -q tests/test_run_baseline.py -m "not gpu and not network"` exits 0; hard-FRAME + no-metric-recompute greps pass.
- GPU (RunPod, manual checkpoint): `reports/baseline_heico_frame.json` exists with `pre_evaluation_score > 0`, `track=="FRAME"`, model-id + judge-mode labels; committed before Jul 15.
- `test -f reports/baseline_heico_frame.json` succeeds after the checkpoint run.
</verification>

<success_criteria>
- MODEL-03: a committed HeiCo FRAME zero-shot `pre_evaluation_score` — measured through the Phase-1 harness, labeled with track/model/judge-mode — exists before Jul 15 and is a valid > 0 floor (unique qIDs, <=300-char adversarial-clean content from the 02-02 guard).
- The metric is never recomputed locally: the driver delegates to `harness.score` and reads `pre_score`/`buckets_df` from the SDK.
</success_criteria>

<output>
After completion, create `.planning/phases/02-zero-shot-inference-engine/02-03-SUMMARY.md`. Record the committed `pre_evaluation_score`, the judge-mode that produced it (`real` vs `no-judge` lower bound), the model-id, per-question latency signal (any >4s flagged for Phase 6), and the path to the committed report — this is the Jul-15 gate artifact and the always-valid submission floor referenced by Phases 6/7.
</output>
